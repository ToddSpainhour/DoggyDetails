
const getMedicinesForThisPet = async (petID) =>
{
    // hit getMedicinesForThisPet endpoint
}

const addNewMedicine = async (medicineDetails) =>
{
    // hit addNewMedicine endpoint
}

const editMedicine = async (medicineDetails) =>
{
    // hit editMedicine endpoint
}

const deleteMedicine = (medicineID) =>
{
    // hit deleteMedicine endpoint
}

export default { getMedicinesForThisPet, addNewMedicine, editMedicine, deleteMedicine }