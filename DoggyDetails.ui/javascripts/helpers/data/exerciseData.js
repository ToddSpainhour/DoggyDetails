
const getExercisesForThisPet = async (petID) =>
{
    // hit getExercisesForThisPet endpoint
}

const addNewExercise = async (exerciseDetails) =>
{
    // hit addNewExercise endpoint
}

const editExercise = async (exerciseDetails) =>
{
    // hit editExercise endpoint
}

const deleteExercise = (exerciseID) =>
{
    // hit deleteExercise endpoint
}

export default { getExercisesForThisPet, addNewExercise, editExercise, deleteExercise }