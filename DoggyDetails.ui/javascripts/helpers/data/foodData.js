
const getFoodsForThisPet = async (petID) =>
{
    // hit GetFoodsForThisPet endpoint
}

const createNewFood = async (foodDetails) =>
{
    // hit createNewFood endpoint
}

const editFood = async (foodDetails) =>
{
    // hit editFood endpoint
}

const deleteFood = (foodID) =>
{
    // hit deleteFood endpoint
}

export default { getFoodsForThisPet, createNewFood, editFood, deleteFood }