
const getNotesForThisPet = async (petID) =>
{
    // hit getMedicinesForThisPet endpoint
}

const addNewNote = async (noteDetails) =>
{
    // hit addNewNote endpoint
}

const editNote = async (noteDetails) =>
{
    // hit editNote endpoint
}

const deleteNote = (noteID) =>
{
    // hit deleteNote endpoint
}

export {getNotesForThisPet, addNewNote, editNote, deleteNote}